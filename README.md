# Data Structures and Algorithms in TypeScript

> Frequency Counter

To test a file, run the command

```
npx tsx <filename>
```

**Note: This is because this project is built in TypeScript and must be parsed for results**