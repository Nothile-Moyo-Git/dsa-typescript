/**
  * Name: Linear Search
  * 
  * Description: Performs a simple search that iterates through the data object once
  * 
  * Date created: 06/08/2026
 */

// =======================================================================================================================
// linearSearch
//
// Iterate through an object and convert all the strings inside it into an array
// =======================================================================================================================
/* const linearSearch = (numbers: number[], value: number): number => {

  const size = numbers.length;

  for (let index = 0; index < size; index++) {
    const number = numbers[index];

    // Return the index if we find it
    if (value === number) {
      return index;
    }
  };

  return -1;
}; */

// console.log(linearSearch([10, 15, 20, 25, 30], 15)); // 1
// console.log(linearSearch([9, 8, 7, 6, 5, 4, 3, 2, 1, 0], 4)); // 5
// console.log(linearSearch([100], 100)); // 0
// console.log(linearSearch([1,2,3,4,5], 6)); // -1
// console.log(linearSearch([9, 8, 7, 6, 5, 4, 3, 2, 1, 0], 10)); // -1
// console.log(linearSearch([100], 200)); // -1